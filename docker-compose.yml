name: fb-bot

services:
  fb-bot:
    build: .
    image: fb-bot:latest
    container_name: fb-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ~/.fb-bot:/app/data
